using SystevoTune.Engine;

// Dev-only test harness for the Engine. Not shipped to users.
// The real front end is the WPF app (Phase 3).

Console.WriteLine($"{EngineInfo.ProductName} ConsoleRunner — engine v{EngineInfo.Version}");
Console.WriteLine("No commands wired yet. Cleanup scan/preview/apply/undo arrives in Step 3.");
return 0;
