<UserControl x:Class="SystevoTune.App.Views.ApplyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="{Binding [Apply_Title], Source={StaticResource Loc}}" Style="{StaticResource Heading}" />
            <TextBlock Text="{Binding [Apply_Subtitle], Source={StaticResource Loc}}" Style="{StaticResource Subheading}" />

            <TextBlock Style="{StaticResource MutedText}" Text="{Binding CurrentTweak}"
                       Visibility="{Binding IsRunning, Converter={StaticResource BoolToVisibility}}" />

            <TextBlock Style="{StaticResource DangerText}" Text="{Binding Error}"
                       Visibility="{Binding Error, Converter={StaticResource StringToVisibility}}" />
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,10,0,0">
            <ItemsControl ItemsSource="{Binding Outcomes}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding TweakName}" FontWeight="SemiBold" />
                                    <TextBlock Grid.Column="1" Text="{Binding AppliedCount}" />
                                </Grid>
                                <TextBlock Text="{Binding Detail}" Style="{StaticResource MutedText}" Margin="0,4,0,0"
                                           Visibility="{Binding Detail, Converter={StaticResource StringToVisibility}}" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Margin="0,12,0,0">
            <TextBlock Style="{StaticResource MutedText}"
                       Text="{Binding [Apply_Cancelled], Source={StaticResource Loc}}"
                       Visibility="{Binding WasCancelled, Converter={StaticResource BoolToVisibility}}" />

            <TextBlock Style="{StaticResource MutedText}" Margin="0,4,0,8"
                       Text="{Binding [Results_NeedsRestart], Source={StaticResource Loc}}"
                       Visibility="{Binding RequiresRestart, Converter={StaticResource BoolToVisibility}}" />

            <Button Content="{Binding [Apply_Cancel], Source={StaticResource Loc}}"
                    Command="{Binding CancelCommand}"
                    HorizontalAlignment="Left"
                    IsCancel="True" TabIndex="40"
                    AutomationProperties.Name="{Binding [Apply_Cancel], Source={StaticResource Loc}}"
                    Visibility="{Binding IsRunning, Converter={StaticResource BoolToVisibility}}" />
        </StackPanel>
    </Grid>
</UserControl>
